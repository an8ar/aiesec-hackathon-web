export type Promotion={
    id: string;
    title: string;
    banner_url: string;
    reviews_number: number,
    reviews_rate: number,
    expires: string,
    discount: number,
    city: string,
    address: string,
    latitude: number,
    longitude: number,
    price: number,
    distance: string,
}
